interface Routes {
  HOME: string;
  LOGIN: string;
  DASHBOARD: string;
  UPLOAD: string;
}

export const ROUTES: Routes = {
  HOME: "/",
  LOGIN: "/login",
  DASHBOARD: "/dashboard",
  UPLOAD: "/upload",
};
